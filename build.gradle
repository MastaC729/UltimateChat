plugins {
    id 'org.spongepowered.plugin' version '0.8.1'
}

group = pluginGroup
version = pluginVersion

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile 'org.spongepowered:spongeapi:5.0.0'
    compile name: 'MCClans-API-1.3'
}

sponge.plugin.id = pluginId
